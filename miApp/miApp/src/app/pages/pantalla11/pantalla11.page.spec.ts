import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Pantalla11Page } from './pantalla11.page';

describe('Pantalla11Page', () => {
  let component: Pantalla11Page;
  let fixture: ComponentFixture<Pantalla11Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Pantalla11Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
