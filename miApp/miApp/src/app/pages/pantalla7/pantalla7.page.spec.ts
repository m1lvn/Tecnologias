import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Pantalla7Page } from './pantalla7.page';

describe('Pantalla7Page', () => {
  let component: Pantalla7Page;
  let fixture: ComponentFixture<Pantalla7Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Pantalla7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
