import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Pantalla8Page } from './pantalla8.page';

describe('Pantalla8Page', () => {
  let component: Pantalla8Page;
  let fixture: ComponentFixture<Pantalla8Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Pantalla8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
