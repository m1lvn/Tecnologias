import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Pantalla6Page } from './pantalla6.page';

describe('Pantalla6Page', () => {
  let component: Pantalla6Page;
  let fixture: ComponentFixture<Pantalla6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Pantalla6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
