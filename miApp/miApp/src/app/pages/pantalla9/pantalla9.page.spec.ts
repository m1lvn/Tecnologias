import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Pantalla9Page } from './pantalla9.page';

describe('Pantalla9Page', () => {
  let component: Pantalla9Page;
  let fixture: ComponentFixture<Pantalla9Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Pantalla9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
